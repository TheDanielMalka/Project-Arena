/** Route-level HUD accent — each maps to a unique decorative layer (visual only). */
export type ArenaPageVariant =
  | "default"
  | "dashboard"
  | "lobby"
  | "leaderboard"
  | "wallet"
  | "profile"
  | "admin"
  | "history"
  | "hub"
  | "forge"
  | "settings"
  | "arena-client"
  | "players"
  | "player-profile";
